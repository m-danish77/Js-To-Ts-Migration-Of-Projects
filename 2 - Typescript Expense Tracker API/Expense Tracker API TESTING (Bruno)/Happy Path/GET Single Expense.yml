info:
  name: GET Single Expense
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/expenses/69efa9ec92bbfd2d17b38dc4
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Should return 200 OK", () => expect(res.status).to.equal(200));
        test("ID should match requested ID", () => {
          expect(res.getBody()._id).to.equal("69efa9ec92bbfd2d17b38dc4");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
